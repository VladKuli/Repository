package org.app.dealer.web_ui.controllers;

/*
@Controller
public class GetAllItemsController {

    @Autowired
    private GetAllItemsService service;

    @GetMapping(value = "/getAllItems")
    public String showAllItems(ModelMap map) {
        GetAllItemsResponse response = service.execute(new GetAllItemsResponse());
        map.addAttribute("users", response.getFinalOrders());
        return "/getAllItems";
    }
}

 */